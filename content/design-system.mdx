export const metadata = {
  title: 'Design System',
  description:
    'A living reference for every component available when writing a post on this blog — typography, links, lists, media, footnotes and more, each shown live.',
  tags: ['design-system', 'components', 'reference'],
  date: '2026-07-19',
  draft: true,
};

## Typography

Four typefaces carry the site, each shown at a representative size.

### Display — Geist

<div className="font-display text-3xl text-foreground">Almost before we knew it, we had left the ground.</div>

### Serif — Instrument Serif

<div className="font-serif text-3xl text-foreground">Almost before we knew it, we had left the ground.</div>

### Mono — Departure Mono

<div className="font-mono text-xl text-foreground">1 AU = 1,495978707 × 10¹¹ m</div>

### Mono, code — Fira Code

<div className="font-mono-code text-lg text-foreground">console.log('foobar')</div>

### Headings

Posts nest levels two through four; level one is reserved for the title.

## Heading level two

### Heading level three

#### Heading level four

### Emphasis

Body copy carries **strong emphasis** for the load-bearing words, _italic
emphasis_ for a lighter touch, and `inline code` for identifiers, file names and
anything you would type verbatim.

## Links

An [external link](https://maximeheckel.com) sits inline in a sentence, right
next to an [internal one](/) — the first carries its site's favicon, the second
does not.

<Anchor href="https://developer.mozilla.org/en-US/docs/Web/CSS" discreet>
  A discreet variant tones the link down for dense reference lists.
</Anchor>

## Blockquote

> A blockquote sets an aside apart from the surrounding prose — a definition, a
> pulled quote, a caveat worth pausing on.

## Callouts

<Callout variant="info">
  Info callout — the default, with the info icon in the corner.
</Callout>

<Callout variant="info" label="Learn more">
  Info callout with a labelled corner instead of the icon.
</Callout>

<Callout variant="danger">
  Danger callout — the alert icon marks it as a warning.
</Callout>

<Callout variant="danger" label="Be careful!">
  Danger callout with a labelled corner.
</Callout>

## Lists

- First item
- Second item
- Third item

1. First step
2. Second step
3. Third step

- Parent item
  1. Nested first
  2. Nested second
- Another parent
  - Nested arrow one
  - Nested arrow two

## Inline Code

`const foo = () => 'bar'`

## Code Block

### Basic

```tsx
console.log('hello world');

/**
 * Some comments
 */
function sayHi(name) {
  const message = `hi ${name}`;
  return message;
}
```

### With title and highlighting

```tsx title="greet.ts" {6-8}
console.log('hello world');

/**
 * Some comments
 */
function sayHi(name) {
  const message = `hi ${name}`;
  return message;
}
```

## Sandpack Code Block

<Sandpack
  template="react"
  dependencies={{ 'framer-motion': 'latest' }}
  files={{
    '/App.js': `import { motion } from 'framer-motion';
import './scene.css';

function WavingHand() {
  return (
    <motion.span
      style={{ display: 'inline-block', transformOrigin: '70% 70%' }}
      animate={{ rotate: [0, 18, -8, 18, 0] }}
      transition={{ duration: 1.6, repeat: Infinity, repeatDelay: 0.6 }}
    >
      👋
    </motion.span>
  );
}

export default function App() {
  return (
    <h1>
      Hi <WavingHand /> !
    </h1>
  );
}
`,
    '/scene.css': `html {
  background: #08080b;
}

body {
  color: white;
}

h1 {
  font-family: sans-serif;
  font-size: 2.75rem;
  font-weight: 600;
}
`,
  }}
/>

## Images

<Image src="blog/halftone.png" alt="A classic halftone gradient" />

## Video

<VideoPlayer
  src="blog/halftone-compilation-compressed.mp4"
  title="Halftone compilation"
  autoPlay
  muted
  loop
  width={663}
  height={398}
/>

## Footnotes

A footnote reference marks a spot in the text <FootnoteRef id="1" />, and the
list at the end resolves it — the reference and the entry link to each other.

## Dividers

---

<FootnotesList
  notes={[
    {
      id: 1,
      content: (
        <p>
          Footnotes are resolved by{' '}
          <a href="https://github.com/LenySauzet/blog.lenysauzet.com">
            the FootnotesList component
          </a>
          , which renders this list and links each entry back to its reference.
        </p>
      ),
    },
  ]}
/>

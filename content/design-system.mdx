export const metadata = {
  title: 'Design System',
  description:
    'A living reference for every component available when writing a post on this blog — typography, links, lists, media, footnotes and more, each shown live.',
  tags: ['design-system', 'components', 'reference'],
  date: '2026-07-19',
  draft: true,
};

This article renders every content component the blog offers, so each one can be
seen live and its pattern copied. It is not documentation — it is a visual index.

## Typography

Body copy is a paragraph. It carries **strong emphasis** for the load-bearing
words, _italic emphasis_ for a lighter touch, and `inline code` for identifiers,
file names and anything typed verbatim.

## Heading level two

### Heading level three

#### Heading level four

Headings nest from level two (`##`) downward; level one is reserved for the
article title.

## Anchor

<div className="flex flex-col items-start gap-4">
  <Anchor href="https://github.com/LenySauzet">@LenySauzet</Anchor>
  <Anchor href="https://youtube.com">YouTube</Anchor>
  <Anchor href="https://developer.mozilla.org">MDN Web Docs</Anchor>
  <Anchor href="/">Home</Anchor>
  <Anchor href="https://github.com/LenySauzet" discreet>
    @LenySauzet
  </Anchor>
  <Anchor href="/" direction="left">
    Back
  </Anchor>
</div>

## Badge

<div className="flex gap-2 flex-wrap">
  <Badge variant="info">Info</Badge>
  <Badge variant="success">Success</Badge>
  <Badge variant="warning">Warning</Badge>
  <Badge variant="danger">Danger</Badge>
</div>

## Blockquote

> Almost before we knew it, we had left the ground.

## Card

<Card title="A card title">
  Cards group related content on a raised surface. The body accepts any markdown,
  `inline code`, links and more.
</Card>

<Card>Without a title, the card is body-only and has no separator.</Card>

## Callouts

<Callout variant="info">
  Info callout — the default, with the info icon in the corner.
</Callout>

<Callout variant="info" label="Learn more">
  Info callout with a labelled corner instead of the icon.
</Callout>

<Callout variant="danger">
  Danger callout — the alert icon marks it as a warning.
</Callout>

<Callout variant="danger" label="Be careful!">
  Danger callout with a labelled corner.
</Callout>

## Details/Summary

<Details summary="Summary: Some short text">
  Content. Some long text nested inside the component. Useful to avoid long,
  optional content. It can take some simple strings or some other custom React
  components. As you want!
</Details>

## Lists

- First item
- Second item
- Third item

1. First step
2. Second step
3. Third step

- Parent item
  1. Nested first
  2. Nested second
- Another parent
  - Nested arrow one
  - Nested arrow two

## Code Block

A fenced block with a title bar (copy button included) and highlighted lines:

```tsx title="greet.ts" {6-8}
console.log('hello world');

/**
 * Some comments
 */
function sayHi(name) {
  const message = `hi ${name}`;
  return message;
}
```

## Sandpack Code Block

<Sandpack
  template="react"
  dependencies={{ 'framer-motion': 'latest' }}
  files={{
    '/App.js': `import { motion } from 'framer-motion';
import './scene.css';

function WavingHand() {
return (

<motion.span
style={{ display: 'inline-block', transformOrigin: '70% 70%' }}
animate={{ rotate: [0, 18, -8, 18, 0] }}
transition={{ duration: 1.6, repeat: Infinity, repeatDelay: 0.6 }} >
👋
</motion.span>
);
}

export default function App() {
  return (
    <h1>
      Hi <WavingHand /> !
    </h1>
  );
}
`,
    '/scene.css': `html {
  background: #08080b;
}

body {
color: white;
}

h1 {
font-family: sans-serif;
font-size: 2.75rem;
font-weight: 600;
}
`,
}}
/>

## Math Equations

Inline math flows in a sentence — Euler's identity $e^{i\pi} + 1 = 0$ — while a
display equation centres on its own line and scrolls if it runs wide:

$$
v_o = \begin{bmatrix}
x\sqrt{1 - \dfrac{y^2}{2} - \dfrac{z^2}{2} + \dfrac{y^2 z^2}{3}} \\[0.5em]
y\sqrt{1 - \dfrac{x^2}{2} - \dfrac{z^2}{2} + \dfrac{x^2 z^2}{3}} \\[0.5em]
z\sqrt{1 - \dfrac{x^2}{2} - \dfrac{y^2}{2} + \dfrac{x^2 y^2}{3}}
\end{bmatrix}
$$

## Images

<Image src="blog/Monet_Meadow-with-Poplars.jpg" alt="Monet's Meadow with Poplars" />

## Before / After Image

<Fullbleed>
  <BeforeAfterImage
    alt="A grayscale source next to its two-colour quantized result."
    beforeSrc="blog/grayscale.png"
    afterSrc="blog/2-color.png"
    defaultPosition={50}
  />
</Fullbleed>

## Video

<VideoPlayer
  src="blog/halftone-compilation-compressed.mp4"
  title="Halftone compilation"
  autoPlay
  muted
  loop
  width={663}
  height={398}
/>

## Footnotes

A footnote reference marks a spot in the text <FootnoteRef id="1" />, and the
list at the end resolves it — the reference and the entry link to each other.

## Dividers

---

<FootnotesList
  notes={[
    {
      id: 1,
      content: (
        <p>
          Footnotes are resolved by{' '}
          <a href="https://github.com/LenySauzet/blog.lenysauzet.com">
            the FootnotesList component
          </a>
          , which renders this list and links each entry back to its reference.
        </p>
      ),
    },
  ]}
/>
